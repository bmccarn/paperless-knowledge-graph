# Reliable evidence-backed queries over historical and current records

Status: generalized plan reviewed at `5d70734`; implementation in progress. Owner: Codex, sole live-data and deployment operator. Baseline: `9d84bd3c0db95fbfa6412ecde609ff25e4de795e` (application tree deployed as `cbdc610`). Requested September 8, 2026: write and review the plan, then implement it and verify the exact reported question through the UI.

## Problem and intended outcome

The exact question is “how has my insurance changed over the years and what is the most current”, in timeline mode. The saved request took 179.3 seconds, found 90 items from 17 documents, audited 12 claims, and withheld the entire answer because two chosen references lacked the precise date values. The needed full-year homeowners dates were in the supplied source span. An indexed older homeowners policy was absent from retrieval. Timeline rejected both proposed events; their original rejection causes were not persisted.

The intended behavior is a useful source-backed answer across document domains: explain recorded changes over time, identify the latest documented observations for each relevant subject, and state unresolved gaps with source links. Insurance policies are one representative acceptance case, alongside bills, contractual terms and medical measurements. It must not require the user to narrow an ordinary question just to work around citation formatting. It must not invent active coverage, current balances or conditions, submission events, supersession, century or precise dates. When only part can be verified, return the independently reverified portion with a clear completeness limitation instead of discarding it or exposing an unsupported draft.

This is a query-layer reliability change, not an insurance-specific rule set. No policy-specific identifiers, insurer names, years, question strings, or fixed policy-family classifier may control acceptance or retrieval. Use existing domain metadata and query-derived subject/type hints only as retrieval signals. Tests must demonstrate the same contracts in at least two unrelated domains, including changing bills/contract terms and dated medical measurements.

The [investigation](../audits/2026-09-08-insurance-history-query.md) distinguishes exact-request reproductions from related controlled defects. All private OCR, identifiers, captured payloads and source checks stay outside GitHub. Synthetic fixtures must exercise the same paths without copying private content.

## Acceptance matrix

| ID | Change | Required proof | State |
| --- | --- | --- | --- |
| D1 | Source date equivalence | Written-month, ISO and configured numeric-order dates compare by valid calendar value and precision; original quotes/offsets remain exact. Invalid dates, compact IDs, incompatible precision, unsupported century and wrong event context remain rejected. | Implemented; regressions and independent review passed |
| D2 | Actionable citation repair | Report missing values/dates/units to the existing repairer; prefer complete supporting passages over a shortest quote that drops needed values. Full-year date beside short-year table works; a date for another application/event cannot authorize the claim. | Implemented; regressions and independent review passed |
| A1 | Verified partial answer | After normal repair fails on fully audited unsupported claims, re-audit the supported subset as a new candidate. Only a fully verified subset can be returned, clearly marked partial, with correct new offsets/digests/citations and bounded omitted-claim diagnostics. | Implemented; regressions and independent review passed |
| A2 | Honest delivery and UI | Ordinary/SSE/persistence/source detail agree. Partial answer has explicit incomplete-answer status, displayed-claim support, and unresolved-gap counts; it is neither shown as 0% verified nor labelled a complete answer. Failed/corpus-changing/incomplete audits remain withheld. | Implemented; regressions and independent review passed |
| C1 | Latest documented versus active | Classify temporal meaning during audit rather than rejecting a word anywhere in the text. A supported latest-within-retrieved-records claim is allowed with the archive-completeness limitation; an active-world claim still needs explicit evidence and must not be inferred from dates alone. | Implemented; regressions and independent review passed |
| H1 | Historical retrieval coverage | Independently query indexed domain documents for history requests, choose a bounded diverse set across relevant subjects/document types and periods, and preserve representative sources through evidence construction and synthesis selection. An older indexed policy survives many recent near-duplicate endorsements. | Implemented; regressions and independent review passed |
| H2 | Source context through selection | Preserve opening and relevant indexed OCR chunks for reserved documents; retain their chunk opportunities in the canonical pack. Rank audit context by discriminating unit terms so common boilerplate cannot hide identifiers or latest-record sources already in the pack. Synthetic cross-domain tests plus replay of the saved live selection. | Implemented; regressions and independent review passed |
| A3 | Audit protocol recovery | One bounded recovery for a nonempty malformed batch response, using exactly the same units and sources. Final batch must contain exactly one valid assessment for each requested ID. Failed/unavailable/incomplete batches remain withheld; expose aggregate protocol diagnostics without source text. | Implemented; regressions and independent review passed |
| H3 | Stage-appropriate source allocation | Keep historical breadth for synthesis/timeline; audit each batch using its assertions and share source opportunities fairly across units. Unrelated historical reservations and a broad leading unit cannot exclude the later units' sources. Same whole-window budget and exact-source checks. | Implemented; regressions, both independent review axes and exact-head CI passed |
| A4 | Answer editor output contract | Generate the repaired prose directly, without an unnecessary JSON envelope. One repair call; empty, failed, cancelled or truncated output remains unavailable. Every nonempty replacement still receives a fresh full audit. | Implemented; regressions, both independent review axes and exact-head CI passed |
| D3 | Formatted scalar equivalence | Compare balanced presentation markup consistently on claim and source copies, preserving raw quotes/offsets and refusing wrong values, signs and units. | Implemented; regressions, both independent review axes and exact-head CI passed |
| H4 | Current-record coverage through packing | Discover dated current-record candidates independently of vector top-k; preserve diverse recent records, historical sources and counterevidence through expansion and bounded packing. Bare identifiers cannot become calendar periods. | Implemented; reviewed plan, focused regressions passed; final code review pending |
| D4 | Decorative source separators | Distinguish repeated field leaders from a numeric sign using certified original context; preserve genuine negatives and ambiguous literal/arithmetic cases. | Implemented; reviewed plan, focused regressions passed; final code review pending |
| A5 | Audited absence versus incomplete audit | A fully audited missing-evidence verdict may be omitted before ordinary subset re-audit; unchecked, failed, conflicting or malformed audits remain ineligible. | Implemented; reviewed plan, focused regressions passed; final code review pending |
| T1 | Timeline dates and diagnostics | Use the same date-equivalence boundary before and after event auditing. Preserve source precision; return structured rejection reason counts in trace without source payload logging. Written dates no longer disappear solely for lacking literal ISO text. | Implemented; regressions and independent review passed |
| R1 | Regression and review | Focused red-to-green tests, actual-datastore suite with zero unexpected skips, frontend lint/unit/type/build/browser checks; independent Standards and Spec reviews and exact-head CI. | Passed: 428 local datastore tests with zero skips; exact source-head CI and both review axes |
| L1 | Exact live acceptance | Deployed exact question in timeline mode yields chronological supported history including the retained historical policy and latest documented terms; cited OCR/current source, event dates, complete delivered ledger and saved conversation verified. Require two independently uncached generations of the unchanged exact question; a fresh conversation alone does not bypass cache. Retain the earlier narrow policy lookup as a separate regression. | v11, v12 and v13 runs failed and are retained; follow-up required |
| O1 | GitOps and data preservation | Immutable reviewed image pins only; actual writer drain before rollout; no ingestion fingerprint change, corpus migration, source writes or model-route/output-cap changes. Runtime controls and separately paused Brain jobs preserved; Flux/image/readiness/health verified. | v13 deployed through reviewed GitOps PR152; data preserved; schedule restoration pending final acceptance |

## D1 and D2: precise dates and useful repair

Create one small source-date module used by answer values and timeline validation. Represent a recognized calendar occurrence with canonical ISO value, precision and original character range. Parse calendar-valid ISO year/month/day, English full/abbreviated month forms and slash dates under an explicit source numeric-date-order setting. Default to month/day/year for this US document archive, document the convention, support day/month/year and reject-ambiguous settings, and include this setting in query cache identity. Never silently infer a different order from the proposed answer. A two-digit year cannot supply a century by itself; use an explicit full-year supporting date or retain source precision rather than pivoting around today's year. Compact unlabelled digit strings are identifiers, not dates.

Numeric verification may consume equivalent date occurrences only after date support is established. Remaining amounts, signs, units, policy identifiers and factual heading numbers still use the existing exact-value checks. Do not join separate quotes to fabricate adjacency or a date. Exact reference membership, raw OCR certification, source digest and offsets remain mandatory. Semantic event meaning remains separately audited; a date comparison does not prove submission, cancellation or a policy change.

Expose bounded structured value mismatch details on each rejected claim, including missing date/value/unit categories. Send those details to the existing answer repairer. Update audit/repair instructions to cite every asserted value and date, include a full-year passage where available, and phrase a record as dated rather than submitted when no submission evidence exists. Do not add an output-token cap or unbounded repair loop. Keep original plus one repair; a partial-subset audit is a separate bounded validation step with no new generation.

## A1 and A2: partial delivery contract

Full supported/qualified answers retain their existing meaning. Add a `partial` disposition for a newly verified subset, distinct from answer completeness. `finalization.complete` remains false for partial; add an explicit delivered-answer verification flag. The delivered claim ledger is complete for exactly the text shown and has its own candidate digest and correct offsets. Preserve the failed candidate's digest, total/supported/omitted counts and bounded omission reasons separately as diagnostics; never attach its ledger to the new text. User-visible gap summaries describe missing verification and count, not unsupported facts presented as conclusions.

Subset eligibility requires a fully audited original or repaired candidate, at least one supported unit and at least one fully audited unsupported or missing-evidence unit, no conflicting or unchecked units, no malformed ledger/invalid attribution, and a stable source snapshot. Never derive a partial answer from timeout, unavailable audit, over-limit/incomplete audit, corpus_changed or unaudited quick mode. Use only the final attempted candidate when that final attempt itself completed a full eligible audit. Never fall back to an earlier fully audited candidate after repair timeout, failed second audit or incomplete replacement. Add the explicit negative regression: complete initial audit followed by failed/incomplete repair must still withhold the answer. Do not silently split a factual unit or remove a negation/condition inside it.

Assemble whole supported units in original order, then invoke the ordinary source auditor against the same full evidence pack with only that subset as answer context. Recompute units, references, source selection, all numeric checks, temporal acceptance and whole-answer validation. Missing antecedents, contradictions and unsupported implied completeness must fail this audit. If it does not wholly pass, keep the ordinary refusal. No recursive subset attempt and no third generative rewrite.

Display a plain “Verified partial answer” notice, supported-delivered-claim coverage, original omitted count, and a clear statement that the answer is incomplete. A nonzero verification indicator reflects the audited text being shown, not the original provider confidence or complete archive coverage. Preserve source detail, restored history, model/mode display and responsive UI. Do not cache partial results as complete successes; a new request must have the opportunity to recover omitted facts. Corpus mutation checks clear all delivered/partial diagnostics that could expose an invalidated answer.

## C1: temporal meaning

Extend the auditor's typed temporal assessment with a documented-state scope and a consistent assertion category: source observation, retrieved comparison, present-world assertion or none. Historical source observations may quote current/active language without becoming present-world assertions; malformed or contradictory category/scope pairs fail. Missing categories retain conservative legacy handling. Require the auditor to distinguish: a historical source observation; the newest observation among the supplied dated records; and a claim of currently active real-world state. A documented-state assertion must identify its comparison scope and supporting sources, consider supplied newer/conflicting relevant records, and avoid claiming the archive is exhaustive. Semantic approval does not bypass numeric/reference checks.

Remove the bare current/latest keyword veto as the decision rule. Preserve conservative handling for actual current-world assertions and unknown scopes. Return an explicit retrieval-limited current-state summary when all delivered assertions are historical or documented-state observations. Do not change `current_state` into a fabricated proof of active coverage. Prompts and acceptance must agree that “latest documented terms found” can answer the user's question while cancellation/replacement remains unresolved.

## H1: retrieve history before polishing the answer

This request's `broad=False` path currently relies on top-ranked planned searches, which did not supply an indexed prior policy. Do not solve this by hardcoding policy IDs, insurers, years or the user's exact question. For timeline/history intent, add a request-owned indexed-document coverage query based on query-derived subject terms and existing source metadata/type/graph hints. Domain is optional: an unknown domain must retain ordinary retrieval and gain subject-based historical coverage without a hardcoded industry vocabulary. Graph/type/name metadata guides retrieval only; certification still uses raw OCR.

Select a bounded deterministic set of candidate documents with diversity across recorded periods and query-relevant subject/type groups, then hydrate relevant OCR chunks in a batched datastore operation. Prioritize direct subject matches in title/type over incidental OCR mentions; fall back to source subjects when metadata is absent. Use existing indexed Paperless dates when source/title periods are unavailable, as retrieval hints only. Reserve temporal/type strata before title diversity or repeated revisions; group population gives no priority. Report omitted strata. Keep oldest/latest representatives plus relevant intermediate records, with stable tie-breaking and a reported coverage limit. Do not perform a full-corpus model pass or embed every document per request. Preserve a source reservation through ranking, the 90-item evidence budget and the 28,000-character synthesis/audit budgets; a reservation guarantees context opportunity, not factual support or forced inclusion in the answer. Repeated excerpts from one recent record cannot displace every older subject/type group. Account for serialized span cost from the outset. History packs use smaller whole source windows so historical reservations and explicitly requested sources share the budget; report requested, selected and omitted priority IDs in synthesis and every audit batch. As clarified by H3, archive-wide historical reservations apply to synthesis/timeline coverage; audit batches select history according to their own assertions rather than reserving every historical document again. Grouping must use existing metadata/query-derived subject hints rather than an insurance-only taxonomy; unknown groups still get temporal coverage.

Test the actual public retrieval-to-synthesis path with a synthetic older policy and many recent endorsement distractors, and real PostgreSQL coverage-query fixtures. Confirm the historical source reaches the model input and audit, not merely an intermediate list. Repeat this regression with unrelated dated invoices and medical measurements. Test irrelevant-subject/domain exclusion, absent type metadata, unknown domains, missing/feedback-blocked sources, cancellation/task ownership and bounded counts. If the indexed candidate set exceeds the coverage limit, report that limitation without claiming exhaustive history.

## H2 and A3: close live source-selection and audit-protocol gaps

The first deployed acceptance run retrieved the older source and validated four timeline events, but still withheld the answer. Its final ledger contained an entirely unchecked four-unit batch, two explicit missing-evidence assessments and an unsupported date. The persisted payload does not retain the original invalid batch envelope, so its exact malformed field is unknown. Offline replay establishes two separate source-selection defects: a reserved document retained only one non-opening OCR chunk and lost its identifying context; a latest-record identifier existed in the canonical pack but its source windows were absent from the comparison batch. These are general source-context and audit-protocol boundaries.

Historical hydration must retain an available opening OCR chunk alongside its bounded relevant chunks. Preserve those selected chunks through the canonical pack budget, rather than reducing every reserved document to one arbitrary excerpt. Reserve an opening source window for document context before repeated excerpts; retain the existing document/item/serialized budgets and report any omitted priority documents. Improve unit-specific and combined audit relevance using discriminating terms (including rare identifiers) rather than unweighted overlap dominated by common source wording. Do not infer support from a matching identifier: the complete assertion still needs semantic and exact-source validation. Prove this with invoice/contract and medical fixtures where opening identity and later dated observations are separated, and with latest-source comparisons among many repetitive notices.

Validate the audit batch protocol independently of its semantic verdicts. A complete batch has exactly one assessment for every requested unit ID, no foreign or duplicate IDs, a recognized status, and a references list. A nonempty response that violates this contract may receive one corrective audit request within the existing request-owned deadline and concurrency. Reuse the same units and full selected source context; provide only bounded structural error descriptions and the exact expected IDs. Do not copy untrusted response instructions into the correction prompt. Empty/unavailable responses, exceptions and timeouts retain their failure handling. Do not retry merely to overturn unsupported, missing or conflicting semantic verdicts. The last response must wholly satisfy the protocol before any of its assessments are admitted. A failed recovery cannot use the earlier batch or an earlier candidate to produce a partial answer.

Record per-batch protocol state, attempt count and bounded error categories in the ledger, without raw model responses, source text or sensitive values. Keep the original-plus-one answer repair limit and independent subset re-audit contract. Update repair guidance to preserve the historical and latest parts of a history question using complete source observations, removing unsupported fields without replacing the requested comparison with an inventory template.

Repeat the complete relevant regression/review/CI gates and GitOps release before resuming exact live acceptance. Preserve the failed live run as evidence; two independently uncached successful generations are still required on the final release.

## T1: timeline correctness and observability

Validate the event's canonical date against exact supporting source occurrences using D1, preserving honest precision. Both proposer references and final auditor references must support the date and event. Reject invalid/ambiguous dates, unsupported precision, mismatched document IDs, wrong references, feedback-blocked sources and unsupported events. Include relevant contradicting sources in the semantic audit as before.

Keep the public accepted event contract and sorted ordering. Add bounded structured rejection reason counts to the timeline trace (for example invalid date/reference, unsupported date, unsupported event, audit unavailable). Do not persist full rejected model payloads or private source text in normal logs. Existing list-return callers remain compatible through a separate diagnostic result interface or an optional diagnostic collector.

## Execution sequence

1. Commit this plan and sanitized investigation. Review the plan against the exact baseline and user request using independent Standards and Spec reviewers. Resolve material findings before implementation; publish the reviewed spec in the repository tracker.
2. Implement D1/D2/T1 with meaningful failing controls, then C1 and A1/A2 through public finalization/delivery seams. Implement H1 through actual indexed retrieval and carry source reservations end-to-end. Add sanitized production browser fixtures for partial/history UI behavior.
3. Update README/config/domain terms where the accepted contracts change. Bump the answer policy/query cache namespace; do not change extraction/identity policies or ingestion fingerprint.
4. Run focused checks during work, then one full required suite on the final implementation. Review both axes, fix findings and repeat affected checks. Publish draft PR with complete source/spec links and exact-head CI.
5. Fetch current application/GitOps remote state. If writers are active, wait for drain rather than interrupting or redeploying over them. If a temporary KG schedule pause is necessary for serial live acceptance, prepare it through a narrowly reviewed GitOps change and restore the original values afterwards. Preserve the separate Brain pause. No live operation is delegated.
6. Merge reviewed source after CI, verify image publication and digest chains, pin both immutable images in GitOps, and verify Flux/readiness/runtime/current source state.
7. Run one exact question at a time in the actual deployed browser; persist conversation IDs before waiting. Inspect visible timeline, partial/full status, sources and restored history. Use a read-only source verifier for every delivered claim/event and current source snapshots. Record model timing and whether the run was cached; do not resubmit after a timeout without checking the retained task/conversation.
8. Accept the exact history question only if it includes meaningful earlier history and latest documented terms or observations for the relevant subjects. A partial fallback that drops the historical or latest portion is not sufficient to close this specific incident. Once a run succeeds, repeat the unchanged question in a fresh conversation and require the same substantive coverage and source checks. Both runs must prove `cached=false`. Use a separately reviewed, bounded eviction of only this exact query cache entry (computed from the current question/history/mode/model/generation/date/date-order identity); do not clear the cache globally or change the question, model or source snapshot to manufacture a miss. Record the cache-key hash, eviction result and uncached receipt privately; also run the separate original current-policy question. Any model failure is retained and analyzed rather than replaced by a convenient easier prompt.
9. Restore any temporary schedule controls via reviewed GitOps, verify health and unchanged history/corpus, update the matrix with actual evidence, and close the tracker only after all relevant rows pass.

## Generality checks

- Date equivalence and value diagnostics are shared by ordinary answers and timelines, including units and signed amounts outside insurance.
- Partial-answer eligibility and revision binding are domain independent; test a supported invoice amount beside an unsupported charge and a supported medical measurement beside an unsupported trend.
- Temporal assessment distinguishes source-relative recency from real-world truth for policies, account balances, medication records and contracts, without special-casing their nouns.
- Historical coverage uses indexed subject/type/date signals and measured budgets. It must work when the query domain is unknown and must expose truncation/coverage limits.
- Keep the exact reported question as incident acceptance, plus a separate cross-domain synthetic end-to-end matrix. Passing either one alone cannot establish the other.

## Exclusions and invariants

No Paperless edits, re-OCR, full sync, Reindex All, blanket vector repair, alias/history deletion, model changes to disguise failures, invented dates/coverage, arbitrary output limits or relaxed raw-source references. Keep existing negative identity/history controls, source feedback/corpus-generation enforcement, finite audit deadlines, request-owned cancellation and original private failure receipts. This work establishes the specified behaviors and representative live case; it does not certify every possible query or exhaustive real-world insurance coverage.


## Reviewed implementation checkpoint

PR #35 merged the H2/A3 follow-up as `f132b1eaa015b1fd3df9fd03a2ad355f2eb46baf`. Exact reviewed source head `9c7b404be4499d79bab946095ee2f1fb0e5f2dc7` passed backend/frontend CI, including production browser contracts. Both independent review axes reported zero remaining findings after their reproduced source-selection and adapter-boundary defects were closed. The local actual-datastore suite passed 407 tests with zero skips. This checkpoint does not establish final production answer acceptance.


## H3, A4 and D3: stage boundaries found in the v12 acceptance run

The first v12 browser run audited all 27 units with structurally valid responses, but only ten were supported; answer repair returned no usable candidate. Current-source reconstruction validated all90canonical items and33retained references. The old identifying passage now reaches the answer, so H2's demonstrated correction remains useful. However, every audit batch reserves the same eight historical openings; the first batch spends its entire27,440-character input on historical sources and omits both latest-record identifiers already present elsewhere in the pack. A broad leading unit can then consume the remaining reservations before more specific units. Offline replay with stage-specific historical reservations and round-robin unit opportunities restores both identifiers to the appropriate batch under the same28,000-character budget.

H3: Preserve historical breadth for synthesis and timeline extraction. Audit batches use the full canonical pack but select their context for their own units. Do not force unrelated archive-history documents into every batch. Keep explicitly requested document reservations, exact-source certification, complementary same-document and cross-document selection, and whole-window serialized budgeting. Build each unit's ordered candidate windows independently, then interleave units fairly before general relevance fill. Account for duplicates only at actual budget admission; one unit cannot assume another unit's late, unadmitted window is present. Audit coverage diagnostics must describe the stage's actual priority set rather than falsely treating all historical documents as mandatory in every batch. Public tests combine a broad initial summary, several narrow later claims, eight historical reservations and competing notices; all source windows needed by the narrow claims fit and must reach their auditor. Preserve old-source delivery whenever an audited unit actually asserts it. Selection must also preserve counterevidence opportunities: for latest/current/comparison assertions, reserve relevant alternative records from the canonical pack, including newer same-subject observations even when their IDs/dates are absent from the proposed claim. Use topical and temporal signals for selection only, never as factual proof or an assertion of exhaustiveness. A public negative control must show a newer same-subject record to the auditor when a stale candidate calls an older record the latest, under competing-source load; the false comparison must remain withheld.

A4: The persisted run cannot distinguish malformed editor JSON from an empty or wrongly shaped response; no timeout/provider exception was logged. The answer editor needs to return prose, so remove the JSON-wrapper requirement instead of adding another generative retry. Use the same configured model, request-owned transport/deadline, and one repair call. Return a nonempty normally completed text result as the replacement candidate; return unavailable on empty output, provider exception, timeout or a provider truncation stop reason. Propagate caller CancelledError and drain request-owned transport; cancellation must not become an ordinary unavailable answer or trigger re-audit, further model work, caching or persistence. Do not parse or salvage partial JSON or fall back to the earlier answer. All replacement prose, including a provider refusal or unexpected formatting, must pass the ordinary complete source audit before publication. Verify through the real Strands adapter with a synthetic transport, including ordinary Markdown containing quotes/backslashes and all failure controls. No output-token cap is added.

D3: A retained exact source quote formats an amount as `$**500.00**`; comparison peels balanced markup on the claim but not on source quantities and incorrectly rejects the matching `$500`. Extract one shared presentation-normalization helper for value comparison on separate claim/source copies. Keep each original certified source quote and its digest/offsets unchanged. Never join reference strings or remove numeric signs, units, decimal punctuation or unbalanced markup. Preserve calendar precision and identifier-boundary controls. Test currency and measurement wrappers, nested balanced formatting, negative amounts, wrong currency/unit/magnitude, split references and raw quoted-source preservation. Before allowing normalized equivalence, validate numeric token and sign boundaries against surrounding source text with presentation delimiters accounted for. Reject chopped wrapped tokens (for example source `$**500**0` with quote `$**500**`, source `1**500** USD` with quote `**500** USD`, and source `-$**500** USD` with quote `$**500** USD`). Unknown/incomplete span-edge context must fail conservatively rather than hide a neighboring digit or sign. Add these reference-validation negatives alongside the positive formatting tests.

Repeat independent plan/code reviews, relevant regressions, complete datastore/CI gates and a GitOps release before final live acceptance. Earlier failed runs remain evidence and do not count toward the two successful independently uncached generations.


D3 review refinement: source presentation decisions are certified as raw marker ranges before any quote is sliced. Use the pinned CommonMark parser to identify actual list blocks and retain original CR/LF offsets, with conservative protection for balanced raw code spans and unknown continuation-chunk context. Literal code/HTML and Unicode whitespace cannot grant list-marker authority. The model cannot provide or override these ranges; source verification reconstructs them. The parser is used only for structural tokenization, never to render or execute source HTML. This replaces the handwritten block detector and remains a query-only change. Reference: [markdown-it-py parser and token maps](https://markdown-it-py.readthedocs.io/en/latest/using.html).


## v13 reviewed release checkpoint

Final source head `4d6c0c536d800dfcb7b822ec495d795f1ee5831e` passed both independent review axes with zero remaining findings, 428 local tests against actual disposable datastores with zero skips, and backend/frontend CI run34277336265. PR36 merged as `3f5b1928069b741ac9adc820ac5d6d52081c24fa`; main run34277678681 passed validation and image publication. Reviewed GitOps PR152 pinned the verified image chains and merged as `74433ebd8792430891c93ade430d73b01065a3e7`. Fresh writer checks showed no registered tasks, steward activity or other database transactions before merge. Flux, exact running image chains, readiness, no-cap/model invariants and all453original review rows passed after rollout.

Independent ingestion activity had increased the corpus before rollout. A new stable baseline of924document hashes,6966embeddings,529review rows and generation2052 was captured; the complete hash digest and all counts/generation remained unchanged across deployment. Temporary KG controls remain0/0/10 until final acceptance; the five separate Brain schedules remain paused. This release checkpoint does not itself satisfy L1.


## H4, D4 and A5: coverage and delivery after v13 acceptance

The retained v13 run completed both candidate audits and validated eight timeline events. Its repaired42-unit answer had33supported,7unsupported and2missing units, all audited. The newest auto declaration was absent from the90-item canonical pack even though indexed discovery contains it. Current-source reconstruction validated all90items and69retained claim references against stable OCR. A repeated decorative dash leader before a positive amount was misread as a negative sign. The missing verdicts made subset delivery ineligible even though the final audit completed. No provider failure was logged.

H4 addresses source coverage before validation. Reuse bounded, feedback-free indexed document discovery for requests needing current/latest observations, including strict mode without history. Select a separate bounded set of recent record opportunities using query-derived subjects, existing types and calendar signals. Compare dates at compatible precision (a month-only title cannot lose to a day-specific record solely because its string is shorter). Keep distinct recent records and cancellation/conflict evidence; titles/types/dates are retrieval hints and never proof of active status. Unlabelled four-digit identifiers, addresses, amounts and durations cannot create calendar periods in either historical or recent selection. Fall back to indexed dates when source/title dates lack calendar context. Existing history reservations remain independent so rich recent titles do not suppress short older sources.

Carry recent reservations and their opening/relevant OCR chunks across context merges, neighbor expansion and canonical packing. After neighbor expansion, enforce document diversity again before repeated chunks consume the remaining item budget. At synthesis/timeline selection, share priority opportunities between recent and historical reservations; an entire history floor cannot starve current sources, or vice versa. Keep existing90-item and28,000-character budgets, explicit requested-document priority, source certification and truthful omitted-priority diagnostics. Recent title/precision queues retain identifiers while removing recognized calendar occurrences. They share opportunities across distinct records and partial-date precision; exact day dates retain chronological ordering within a queue. Title equality never authorizes discarding a different dated record, and source-only relevance can outrank older metadata matches. Missing titles do not collapse every record into one family. Repeated recent source windows with identical text may reuse synthesis context already supplied by another record; preserve all canonical records, historical reservations and explicit/audit source opportunities. Start with at most8recent and8historical document opportunities, deduplicated, using whole-window admission; this is bounded retrieval, not exhaustive archive certification. Prove the path with two independent subjects in both invoices and measurements, an older short-title record, several distinct latest records/counterrecords and many repeated neighbor chunks. The desired latest sources must reach actual synthesis and the relevant audit batches, not merely an intermediate candidate list.

D4 treats an unambiguous repeated dash field leader as presentation only on comparison copies. Require a bold uppercase multiword display-field label, same-line source-certified context and at least three repeated separated dash marks before a value; other dashed prose remains ambiguous. Never reinterpret a single sign, a sign directly attached to a value after a leader, code/HTML literal content, a mathematical expression, an unknown continuation boundary or arbitrary whitespace as a positive amount. Preserve original quotes, offsets, digests and precise values. A quote cannot create its own leader context by slicing away a preceding sign or literal delimiter. Test positive formatted field leaders and negative/ambiguous controls with unrelated charges and measurements. Currency/unit inference from an unrelated table value remains disallowed.

A5 distinguishes a complete semantic verdict of missing support from a missing audit result. After the existing one repair, a final fully audited ledger containing supported plus unsupported/missing units may form a whole-unit supported subset. Missing-only omissions are also eligible. The ordinary subset audit must revalidate the entire delivered revision, values, references, antecedents and temporal meaning. All existing exclusions for conflicting/unchecked units, invalid attribution or scope, malformed protocol, failed repair/audit, cancellation and corpus mutation remain. Missing and unsupported units are omitted, counted and never shown as verified facts. Add positive cross-domain missing-omission tests and negative incomplete/malformed/conflicting controls.

Review this extension before implementation. Repeat source/code review, full checks, GitOps and both exact uncached history generations plus strict current-state and browser/source acceptance. The v13 failure is retained; no successful result has yet satisfied L1.


H4/D4/A5 plan review at `211d070` passed both axes. Initial public regressions reproduced missing-current discovery, a title identifier creating a false calendar period, false negative amounts from field leaders and withheld audited-missing subsets. The implementation passes those controls plus the existing history/source-boundary regressions. Offline packing of the saved current-source dataset now presents the old policy, both latest declarations and cancellation requests in27,761serialized characters; exact-source field-leader value mismatch is empty. This is an offline selection/value check without a model replay, not live answer acceptance. Final code review and full validation remain pending.


Code review of `0184995` reproduced abbreviated units escaping the retrieval calendar guard, exact days being discarded during recent ranking, generic titles erasing independent records, newer OCR-only subjects losing to older metadata matches, and conventional field-label colons failing scalar equivalence. These are closed with one shared calendar-year/unit predicate, recency-first ordering with separate precision opportunities, nondestructive title queues, and source-certified colon variants. Public QueryEngine regressions inspect actual synthesis and auditor input for the recency failures. Repeated identical recent windows no longer consume the separate historical context allowance. The existing history/explicit-document coverage contracts remain tested. Final review/full-suite results follow separately.

A second review found partial dates still losing across distinct title queues, long-form/subsecond duration spellings escaping the shared year guard, and equations being mistaken for decorative field leaders. Selection now shares opportunities between overlapping date precisions within calendar cohorts while retaining exact-day ordering. The shared guard covers standard duration spellings. Field-leader authority requires the entire remaining field to be one displayed scalar; equations, arithmetic and following prose are ineligible, including when a quote trims away that context. The 48 focused boundary/coverage tests pass, and the saved-source selection still carries the old/latest/cancellation records within27,874serialized characters. Final review and full-suite gates remain pending.


Final boundary review extended the equation control to wrapped continuation lines and the positive measurement control to independently balanced value/unit wrappers. Field-leader authority requires each line in a CommonMark inline block to be an independently complete display field and validates each full normalized scalar while preserving its sign. Adjacent fields remain eligible; a wrapped arithmetic or prose continuation invalidates the paragraph. The 21 focused source-format tests pass. Actual read-only mobile restoration also reproduced a numeric-zero conditional rendering defect: saved answers with a zero score and no duration omitted their score/copy controls and rendered a stray zero. Explicit presence checks and a restored-conversation browser regression cover this delivery boundary. These checks do not count as live answer acceptance.


A positive two-field paragraph control caught an overrestrictive single-line-block guard. The final parser admits multiple independently complete form fields while retaining wrapped-equation negatives, with full and trimmed reference tests for both fields. All22focused source-format tests pass. Frontend lint,12unit tests,type checking,production build and all production browser contracts passed, including saved zero-score restoration.


## A6/D5: source and audit boundaries exposed by v14 acceptance

The first v14 run reached the old and latest declarations and validated17timeline events, but its repaired15-unit answer had9supported,4unsupported and2missing units. All15were audited. The supported subset was attempted; one batch returned an unavailable result, and the answer stayed withheld. The retained payload cannot distinguish absent text from a normally completed falsey JSON value. Current-source reconstruction confirms all81pack items and43retained claim references against stable OCR. A person's middle initials split one sentence into orphan fragments. A correctly quoted positive field in a continuation chunk lacked structural context, so the v14 presentation fix could not apply. These are general query-boundary failures; source-invented currency, expanded model numbers and unsupported status transitions remain invalid.

A6: Preserve semantically complete answer units across personal initials and ordinary abbreviations, with exact offsets and no dropped prose. A supported subset must not publish fragments created by a bad sentence boundary. Keep independent sentence boundaries and existing source/temporal re-audit. Exercise multiple unrelated names, headings, numbered prose, decimals and abbreviation controls through finalization and subset delivery.

Distinguish unavailable provider text from a nonempty normally completed response that parses to an empty object/list or another invalid JSON shape. At the source-auditor adapter boundary, retain a bounded structural-failure marker for every such nonempty invalid result so the existing one structural correction can run; do not add an extra correction or retry valid negative verdicts. Empty text, transport failure, truncation, timeout and cancellation keep their fail-closed behavior. Record safe fixed failure categories and durations without raw model/source text. If subset verification fails, retain its complete audit diagnostics and report the actual failure instead of attributing the result solely to the prior candidate's unsupported claims. Never reuse prior support as delivered verification. Native-adapter synthetic transport tests cover falsey JSON shapes and failure controls; public finalizer tests exercise subset correction and failed correction.

D5: Structural source authority must follow original document context rather than assuming only chunk zero can establish it. Reuse full original OCR already fetched by bounded source expansion; keep it request-local and never add it to model context or persisted/public payloads. For an evidence chunk that occurs exactly once as a contiguous substring of that original document, parse presentation structure on the full original text and project certified raw marker ranges into the chunk. Bind the certificate to the full document digest and exact offset, preserving original chunk/quote bytes and existing identities. Carry this authority consistently to indexed and expanded chunks of the same fetched document. Parse each distinct full source once per span build. Missing/ambiguous/noncontiguous matches, unknown source context, stale certificates and literal code/HTML cannot grant authority; retain conservative existing behavior when full context is unavailable. Ingestion, chunking and stored data are unchanged.

Public invoice/measurement tests must exercise a field and list marker in a later chunk, including enclosing literal syntax that begins before the chunk, duplicate chunk occurrences, mismatching full-source context and preserved raw quote offsets/digests. The actual QueryEngine path must carry the certificate through canonical packing, source audit and public metadata without exposing full OCR or expanding existing source/context budgets. Reconstruct current-source certificates independently during acceptance.

Repeat plan/code review, focused and full validation, reviewed GitOps deployment and L1. The failed v14 run is retained and does not count as acceptance. Its observed approximately362second duration is recorded; no output cap or model routing change is proposed.


A6/D5 plan review at `9926cf1` passed both independent axes. The implementation preserves single-letter initials (including Unicode names), distinguishes nonempty falsey JSON through the actual Strands transport, and retains failed subset audit claims/summary/protocol state with an incomplete disposition. Safe stage outcomes and queue/execution durations are logged without response text. Source expansion shares request-local original context across retained indexed/expanded chunks; unique digest/offset bindings project source-certified presentation ranges and source-adjacent numeric/date guards. Invalid/ambiguous bindings cannot fall back to chunk-zero authority. Public metadata contains only the binding, never full original text.

All 61 focused tests pass, including actual QueryEngine packing, native SDK protocol correction, failed subset diagnostics, unchanged signs/literal boundaries and name-fragment delivery. The saved current-source value that failed in the v14 continuation chunk now passes with its exact original quote and full-source certificate, without model replay. Final independent code review, complete checks, deployment and L1 remain pending.


The first implementation review reproduced overmerged terminal letter values, premature ordinary-abbreviation/decomposed-initial splits, duplicate full-source parsing and an incomplete failed-subset manifest. Boundary inspection now uses normalized copies plus bounded name continuation, preserves raw offsets across a wrapped initial, and keeps independently terminated grade/vitamin/apartment facts separate. Public partial-delivery tests cover both omission and retention, Unicode composition parity and ordinary abbreviations. Full-source tokens are shared between structural checks; failed subsets retain their distinct source manifest/selection coverage with the same content stripping as the primary ledger. All49affected review checks pass. Final review and full validation are still pending.


The next review extended the name controls to leading initials, CR-only wrapping, multiple wrapped initials and wrapped abbreviations. Boundary inspection now retains the pending unit context across LF/CRLF/CR continuations, and leading initials can use following name-shaped context while explicit sentence openers preserve independent facts. All49affected checks pass, including public fragment-omission regressions. Invalid empty source certificates also cannot fall back to chunk-local authority. Final review and validation remain pending.


Final boundary review combined the previously isolated cases: ordered prose under a pending heading and leading initials before lowercase surname particles. Ordered-marker recognition now uses the current line while semantic continuation retains the pending unit context. A135-case matrix crosses headings/labels, numbering/bullets, three line endings, initials and abbreviations; every raw unit range remains exact. Public subset controls also cover leading surname particles. These changes remain domain-independent. Final independent review and full validation follow.


A further independent-letter control showed that right-side capitalization alone was insufficient when a new sentence began with a content noun. Initial continuation now also requires left-side name context: a leading initial, a prior name/initial or a generic name-introducing relation. The rule does not enumerate document domains or letter-value labels. Public subset regressions retain supported letter facts before unrelated unsupported sentences.


Name inspection now tracks prose separately from its raw heading/list range, preserving leading and wrapped initials under structural prefixes. Coordination carries prior name context rather than treating bare `and` as proof of a name. Generic copulas and reporting predicates cover ordinary name introductions. Ambiguous name-like prepositional constructions stay atomic; text-only sentence segmentation cannot certify linguistic meaning, and full source/temporal audit remains mandatory. The combined boundary matrix now contains189cases plus public subset controls for introductions, coordination and independent letter values.


## A7: presentation labels belong to their following claims

A section label expressed as an isolated bold paragraph has the same role as a Markdown heading or colon label. It must retain its exact raw text and attach to its following factual unit, including in a supported-subset revision. It must not become a separately publishable fact or independently consume a source verdict. Recognize whole-line strong-emphasis labels using Markdown structure rather than particular topic words; cover both supported strong delimiters and headings combined with lists. Preserve substantive emphasized sentences as factual prose, include any factual meaning in a label in the joined audit unit, and retain trailing labels for audit when no following claim exists. Never discard a failed fact merely because it is styled as a label.

Use generic invoice and measurement examples through public finalization: auditors receive the exact label-plus-claim unit, an unsupported labeled claim is omitted together, supported labeled observations survive subset re-audit, and factual labels/terminal emphasized sentences cannot bypass source checks. Check exact raw offsets, blank lines, line-ending variants, headings, lists and trailing labels. Preserve existing name, numeric, source-certification and temporal invariants. No new model retries, source changes, or output limits. Review the plan and code, repeat full checks, deploy through reviewed GitOps and repeat the complete live acceptance gates.


A7 plan review passed both axes. The implementation uses full-answer CommonMark structure so code/HTML literal blocks cannot create label authority. Generic invoice/measurement finalizer regressions failed before the change and pass with exact label-plus-claim units and fresh subset verification. Review additionally covered sentence punctuation followed by closing quotation or bracket syntax; those emphasized sentences remain independent. Unpunctuated strong-only paragraphs are conservatively kept atomic with following prose, and all their factual meaning remains subject to source audit.


## A8: preserve subject context when delivering a partial answer

A supported scalar is not a useful or safe answer if filtering disconnects it from the record, subject or dated observation it describes. Model source support alone cannot certify that a renderer preserved these associations. Partial delivery must preserve the original Markdown structure and required context, rather than concatenating surviving unit strings into a new flat list.

Derive conservative structural dependencies from the complete canonical candidate: nested list items depend on their governing parent item; section/list labels govern their following field units until an explicit structural scope ends. ATX/setext heading depth and nested list containers provide boundaries; sibling key/value field items do not reset a record scope. Ambiguous flat field runs retain their prior dependencies or are withheld with dependency_unknown. Preserve original indentation and structural separators. If a governing unit is omitted, omit its dependent descendants as well; never move them under a different surviving heading or record. Independent, self-contained observations remain eligible. Reparse the exact assembled subset and require every surviving unit to retain its original governing section/container context; copied indentation alone is insufficient. Do not copy any unsupported parent text back into the result or use a shared source document alone as proof of shared subject. Re-audit the exact resulting revision, including subject, dates and scope, through the existing subset pass. No additional model retry is introduced.

Synthesis and the existing repair step should produce concise self-contained source-observation bullets, each naming its subject/record and relevant date or term. Avoid detached field inventories for broad record/history questions; this reduces dependence on a heading that may later be rejected. Keep source-backed identifying observations even when an ancillary field must be omitted. The auditor must explicitly reject scalar/field fragments whose subject or temporal association is unresolved after filtering.

Retain diagnostic information for omitted units, including their exact candidate text, rejection/value mismatch reasons, and structural-dependency omissions. These remain explicitly omitted diagnostics, never verified answer facts. Preserve source privacy boundaries and exact candidate/answer digests.

Public cross-domain controls must reproduce two-record answers where either the first record header or a later peer header is rejected while otherwise supported child amounts/names remain, and prove those children cannot inherit a different record's heading. Cover nested and flat section/field lists, multiple sections, supported ancestor chains, independent sentences/bullets, original indentation, source/temporal conflicts and invalid/unknown dependencies. Verify the final persisted answer and UI render retain the subject association. Repeat review, full validation, GitOps and real-corpus acceptance; exact-reference checks alone are insufficient without manual semantic coverage review.

A8 plan review passed both axes. Public synthetic controls reproduced the lost-parent and backward-reassociation defects before implementation. The revision preserves raw layout, prunes transitive dependencies, compares reparsed ancestry with multiplicity, and withholds ambiguous quotation/literal containers. Audit context keeps indentation and paragraph boundaries while bounding redundant empty lines. The earlier name-boundary partial-delivery fixture now explicitly closes its rejected heading scope before the independent statement; retaining a fact under a rejected governing heading is no longer an accepted behavior.

A8 follow-up: field classification must distinguish a key/value delimiter from punctuation inside an independent observation, including clock values. A time-bearing dated observation must not inherit an unrelated preceding claim merely because its text contains a colon. Preserve ordinary field prefixes whose values contain times, and retain the unchanged fresh subset/conflict audit. Generic contract-notice regressions reproduce the lost independent observation before the correction.

Treat bare clock-shaped numeric keys conservatively. Exempt only clocks introduced with explicit temporal context; preserve subsequent field delimiters, decimal/quantity forms, and time-keyed fields. A rejected record must still remove its numbered fields and their following dependents.

## A9: source selection is an identifier contract

The auditor's job is to decide whether supplied source passages support an assertion. Recreating source identity tuples and quoting their text is an avoidable second task. Let the production auditor select references as exact supplied `span_id` handles. Resolve each handle against only the canonical spans supplied to that audit batch; the application owns document/evidence identity, original source text, offsets, digest and presentation metadata. Pass the resolved original passage through the same source-boundary, feedback, value, date and temporal checks. Unknown or unselected handles never resolve. A handle with conflicting or extra identity/quote fields must not silently discard those fields: validate the complete legacy reference or reject it. Retain the existing exact-reference interface for legacy callers; do not loosen its acceptance rules.

The semantic auditor must check all assertions, subject, dates, quantities and relationships against the selected passage, including counterevidence. A handle is provenance, not an automatic supported verdict. Keep the existing bounded passage selection, complete audit protocol, deadlines, retry count and raw-OCR certification. Selecting an entire supplied passage must not grow it into a whole document or concatenate disjoint text. Final ledgers and source UI continue to contain resolved original quotations and their exact offsets.

Raw retrieval windows can begin or end inside a token. Publish citation-safe contiguous ranges before auditor selection: trim incomplete edge tokens inward, derive offsets and all source/presentation guards from the original certified window, and require the complete resulting passage to pass the existing reference validator. Do not expand the window, join passages or relax numeric/sign boundaries. Preserve legacy window construction for legacy callers; production answer/timeline auditors and independent source reconstruction must use the same canonical safe-range construction. Report windows that cannot provide a valid safe range as unavailable citation opportunities. Include a long single-paragraph source whose 4,000-character window cuts a word, plus clipped signed/decimal/marked-up numeric edge controls; a complete interior fact must remain citable without accepting an edge fragment.

Persist bounded reference diagnostics that distinguish an empty reference list, unknown/unselected span, identity mismatch and rejected source quotation/boundary. Retain the model's original verdict separately from the final verdict. Do not log source text or publish rejected references as verified facts. Empty references on a missing-evidence assessment must not be described solely as malformed citation text.

Public controls must exercise identifier-only references through full finalization, exact canonical quotes and digests, wrong/unselected/feedback-blocked handles, mismatched legacy identities and quotes, numeric/date/sign/unit failures, conflicting sources and unchanged temporal gates. Use unrelated invoice and measurement records, including a wrong value elsewhere in a passage that the semantic auditor must reject. Confirm provider payload and actual Strands transport contracts with synthetic sources.

## A10: repair produces independent observations before presentation

Free-form Markdown is useful presentation, but it does not reliably identify whether a labeled field is an independent record observation or a dependent scalar. Give the existing single repair call an explicit candidate contract: a JSON object containing a nonempty list of plain-text `observations`. Each observation must answer part of the question, name its own subject/record and relevant date or term, and contain only the identifying or changing facts required by the question. No separate headings, detached field inventories, administrative detail or cross-observation antecedents. This supersedes A4's production prose-return contract; it does not add a repair retry or restore an output-token cap.

The application validates the complete structured response and renders observations as independent top-level prose items. Treat each whole observation as one atomic audit unit. Do not infer its dependencies from colons, initials, numbers or incidental Markdown. A small candidate module should own canonical rendering, units/ranges, audit framing and subset selection, hiding the representation choice from finalization. Existing arbitrary-prose candidates retain the conservative A8 path. Legacy repair adapters may still return prose and must not acquire structured-observation privileges.

Audit each structured observation as self-contained: no surrounding answer text or sibling observation can supply a missing subject, antecedent or temporal context. Multiple assertions within one observation all require support; do not split out a convenient supported fragment. An unsupported observation can be removed only as a whole. Re-render the remaining observations, recompute unit IDs/offsets/digests and re-audit that exact candidate through the existing single subset pass. No facts or labels may be copied from rejected observations, and no failed/incomplete/conflicting audit becomes partial delivery. Keep stable source snapshots, source/value checks and current-versus-documented semantics unchanged.

Record the candidate unitization strategy with the ledger so persistence, source verification and restored UI can validate the exact same units. Reject malformed/empty structured responses, non-string items, embedded block structure, invalid ranges and observations exceeding the existing audited-unit bound; do not truncate or silently drop malformed items. Cancellation, timeout and invalid repair still withhold the answer without falling back to an earlier candidate. This is a representation contract, not a domain classifier.

Cross-domain public regressions must prove that rejecting one invoice/measurement observation preserves independently supported earlier and latest observations, that scalar/pronoun fragments are audited without inherited context and rejected, and that compound unsupported observations are removed atomically. Cover exact rendering/ledger/citation binding, structured-repair protocol failure, legacy fallback safety, conflicting or failed subset audits, unchanged model call counts, SSE/persistence/cache/source UI, and current-source acceptance tooling. Review this plan and implementation on both axes, run the required full suites, deploy through reviewed immutable GitOps pins after writer drain, and repeat the complete live acceptance gates. Keep all previous failed live runs as failures.

## A11: enforce and diagnose the structured repair protocol

A structured output contract must be represented in the provider request, rather than relying only on prose instructions. For the existing single answer-editor call, send an explicit JSON Schema response format for an object containing a required array of independent observation strings and no extra properties. Keep the schema beside the observation parser. Configure it on the request-owned model instance; do not mutate shared configuration or alter other model calls. This is a representation contract: no new output-token cap, model route, retry, source privilege or fallback to a prior candidate.

The installed Strands OpenAI adapter forwards model `params` into the wire request. LiteLLM documents JSON Schema response formats for Gemini and other compatible providers; Gemini's structured-output documentation still requires application-level value validation. Keep strict whole-response JSON parsing, duplicate-key rejection, plain single-line observations, the existing audited-unit bound, exact rendering, independent semantic/source audit and all partial-delivery exclusions. An unsupported provider response format must fail closed rather than silently falling back to unconstrained text. References: [LiteLLM structured outputs](https://docs.litellm.ai/docs/completion/json_mode), [Gemini structured outputs](https://ai.google.dev/gemini-api/docs/structured-output).

A failed repair must expose a bounded, typed diagnostic without retaining source or generated answer text. Distinguish an unavailable transport from invalid JSON, duplicate keys, invalid object/list shape, empty observations, invalid item types, empty/padded/multiline/oversized items and disallowed markup. Keep optional item indexes and aggregate counts only; never interpolate parser/provider exception messages or content into public diagnostics. Persist the diagnostic with terminal finalization so ordinary responses, SSE and restored conversations explain the same failure. Do not report an inferred retrospective syntax cause when the original response is unavailable.

If a valid repaired candidate exceeds the existing audit-unit capacity, preserve the incomplete disposition and withhold the whole candidate, with an `audit_unit_limit` diagnostic recording only unit count and capacity. Do not truncate observations or change output limits, audit capacity or retry behavior. Unavailable adapters and absent repair responses report `transport_unavailable`; malformed legacy response objects report `invalid_object` while valid legacy prose remains supported. Decoder value failures are sanitized as `invalid_json` without erasing the distinct typed duplicate-key error.

Native SDK transport tests must prove the schema is present only on the editor request, other overlapping requests do not inherit it, all output caps remain absent, the valid structured response receives its fresh complete audit, and malformed/truncated/failed responses stay withheld with the correct typed diagnostics. Retain legacy prose-adapter behavior. Review this extension before implementation, then repeat both code-review axes, required validation, immutable GitOps deployment and the unchanged live acceptance gates. All earlier failed acceptance runs remain failures.

## H5 and A12: align evidence opportunities with verification requirements

Source selection must recognize the same conservative calendar equivalence that verification requires. Raw word overlap can prefer a short-year table while excluding an available full-year passage for the same record. Add request-owned calendar features from the existing date parser to the per-document and per-unit relevance queues, using the configured numeric date order and honest precision. Keep lexical identity and complementary source windows; full-date opportunities must not erase the subject or record that gives a value meaning. Do not expand a two-digit year, reinterpret compact identifiers, invent a calendar value or use a matching date as semantic proof. Include bounded aggregate available/selected/omitted date-opportunity diagnostics in the audit selection record without generated/source text.

Comparisons also need diverse relevant alternatives. An old record's future expiration or renewal date must not monopolize counterevidence selection as though it were the document's recency. Preserve a first relevant window from distinct eligible alternative documents before spending their reserved opportunities on repeated continuation windows; use existing request-owned recent-record hints and topical relevance as retrieval signals, never factual authority. Keep complementary supporting windows interleaved fairly with alternatives across the actual audit units. Preserve whole-window serialized budgeting, explicit-document requests, feedback exclusions and deterministic ordering. Record the relevant alternative-document set and omissions so a bounded selection cannot appear exhaustive. Send each unit's known omissions to its auditor; an unqualified comparison must not be accepted as covering eligible records that were not supplied. Preserve this scope restriction independently of the model verdict. Calendar values may be secondary ordering hints within the topical cohort, but must not determine document eligibility or masquerade as document issue dates. Do not increase budgets or introduce another retrieval/model pass.

Remove the repair instruction that unconditionally substitutes a newest-source-backed comparison when latest/current status cannot be established. An unresolved comparison should be rebuilt as self-contained dated observations of the relevant competing records, retaining the identifying and temporal facts needed to answer the question. Preserve the existing application-owned current-status qualification; the editor must not create another disclaimer unit. Supported comparisons among retrieved records remain permitted; dates alone do not establish active status, uniqueness, cancellation, replacement or completeness. Preserve the final conflict gate and the one-repair limit. No word blacklist, case-specific record classifier, deterministic prose surgery or fallback to an earlier candidate.

Reproduce both selection defects with generic service/contract and measurement examples through public finalization: separated identity and full-date passages, abbreviated date tables, equivalent month/ISO/numeric dates, invalid dates, wrong-event negatives, and an actual newer status record behind long older records with future term dates. Assert the necessary source windows reach the auditor within the existing budget. Add budget-pressure controls where known alternatives are omitted and a model-supported unqualified comparison must remain withheld; verify false comparisons remain withheld, unsupported centuries still fail and exact source offsets/digests remain intact. Run an offline selection check against the saved failed request without invoking a model. Review the plan and implementation independently, repeat required validation, release only through reviewed immutable GitOps pins, and repeat all unchanged live acceptance gates. Prior failed runs remain failures.

Review clarification: a document is not fully supplied merely because one of its passages is selected. Track actual admission of its available comparison windows, including continuations and windows in the supporting document; partially supplied documents remain known omissions. Compute these opportunities independently of wording used to prioritize comparison retrieval. Apply the omission restriction to every documented comparison. Neither a phrase such as "compared with", absent superlative wording, presentation markup, nor source IDs mentioned in prose proves a closed comparison scope. This patch adds no exclusive source-selection contract: pairwise comparisons remain eligible when their known relevant opportunities are supplied, while comparisons with omitted opportunities are conservatively withheld. Independently audited dated observations remain eligible. Add public regressions for omitted status continuations behind selected openings, broad comparisons phrased without a recognized superlative, and presentation-equivalent spacing/markup.

## T2: publish dates only from the final verified observations

Timeline presentation must not introduce factual assertions independently of the final accepted answer. Replace the separate event-generation/audit path with a deterministic projection of calendar occurrences in the final delivered claim ledger. Remove independent event prose from synthesis and remove timeline-only source augmentation. This supersedes T1's generated event contract and the earlier timeline-only-source expectation in `answer-review-hardening.md`. Retrieval breadth, chronological answer instructions, source certification, current-state qualification and the existing answer repair/audit limits remain unchanged. No domain-specific status rules, added model calls, retries or output caps.

The timeline module owns candidate eligibility, exact digest/range binding, date recognition, source-date support, deduplication, sorting, diagnostics and restored compatibility. Project only a fully audited supported/qualified answer or the independently re-audited supported subset of a partial answer. Preserve the exact canonical candidate in its accepted ledger; bind every card to that candidate digest and claim ID/range. Each card repeats the complete exact verified claim and every validated reference belonging to that claim. It cannot supply replacement titles, summaries, a single privileged document or facts from an omitted/earlier candidate. Failed, unaudited or incomplete candidates publish no verified cards.

Use the existing source-date parser and configured numeric order. Record the raw occurrence, offsets, normalized calendar value and original precision, requiring support in the claim's validated source quotes. Preserve source identity/context and reject invalid dates, ambiguous dates, unspecified centuries and calendar-shaped identifiers. Bare years require calendar context, including explicit year ranges; quantities and vehicle/model years alone are not calendar events. Deduplicate equivalent occurrences within one claim, but preserve distinct dates and observations. Sort by the normalized date without manufacturing a January 1 or first-of-month fact. There is no separate model binder or generated prose.

Present the section as dates mentioned in verified observations, explaining that the listed date may be a request, issue, effective or expiration date and does not alone prove an action occurred. Show the full claim and all source links for every entry, with wrapping at mobile widths; remove the silent six-entry cutoff. Multiple dates in one observation may produce multiple chronologically sorted cards, each preserving the complete context. Do not imply that the first/last date establishes active status. Distinguish a valid answer with no qualifying dates from invalid projection, and keep projection failure local to the timeline while preserving verified prose.

Version the projection and bump query cache policy. Ordinary, streamed, cached and restored delivery must carry the same candidate-bound cards. Validate stored binding against the exact accepted candidate, final answer digest and ledger; historical unbound events are not displayed as verified cards. Preserve stored conversation rows and expose a short unavailable notice. Invalid required timeline projection cannot be cached as a successful Timeline answer or satisfy live acceptance. Preserve the final corpus-snapshot check and cancellation propagation; there is no asynchronous timeline work to leave running.

Cross-domain regressions must cover requested versus completed transitions, several date roles, unsupported/omitted/repaired observations, stale/swapped candidate digests and ranges, reference/date association, full multi-source citations, year/month/day precision, date-order and identifier negatives, valid no-date answers and failed/Quick audits. Exercise public QueryEngine, SSE/cache/persistence/restoration, late feedback/corpus changes, and more than six full accessible cards on desktop/mobile. Remove tests that certify the retired independent event path; retain their meaningful source/date/counterevidence controls through the final answer and projection interfaces. Review this plan and implementation on both axes, run required full checks, deploy through immutable reviewed GitOps pins and repeat the unchanged live acceptance gates. Previous failed live runs remain failures.

T2 review clarification: only `observations_v1` carries the independent-observation contract. Its cards show complete atomic observations. For `prose_v1`, dated units may depend on other accepted headings, subjects or antecedents: display a neutral chronological date index with all inherited source links and navigation to the full accepted answer already rendered once. Do not display a detached claim excerpt as a standalone assertion or parse new semantic dependencies. The event still binds its exact claim/range for verification, with an explicit answer-context presentation type. Test two named records with dependent dated fields and reverse chronological source order. Restoration validates historical stored binding, not current-corpus recertification.

T2 restoration also verifies the candidate-to-answer relationship: the shared deterministic citation renderer and exact application-owned partial/current-status suffixes must reproduce the saved answer. Matching unrelated candidate and answer hashes independently is insufficient. Swapping a valid ledger between different saved answers must fail locally.
